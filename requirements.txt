# Runtime dependencies for the AI-Powered Dynamic Hotel Pricing System.
# Pinned with lower bounds; for fully reproducible builds, compile a lockfile
# (e.g. `pip-compile`) from this file.

# Web / API
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
python-multipart>=0.0.9

# ML / data
xgboost>=2.0
scikit-learn>=1.4
optuna>=3.6
mlflow>=2.12
pandas>=2.2
polars>=0.20
numpy>=1.26
pyarrow>=15.0
joblib>=1.3

# Scraping
playwright>=1.43
crawl4ai>=0.2
beautifulsoup4>=4.12

# Agents / LLM
anthropic>=0.25
langgraph>=0.0.40

# ETL / scheduling
prefect>=2.18
apscheduler>=3.10

# Persistence
sqlalchemy>=2.0
psycopg[binary]>=3.1
redis>=5.0

# Observability / utilities
prometheus-client>=0.20
loguru>=0.7
pyyaml>=6.0
httpx>=0.27
